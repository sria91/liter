[package]
name = "sqlite3-shell"
description = "SQLite3-rs: interactive command-line shell (port of sqlite3 CLI)"
version = "0.1.0"
edition = "2021"
publish = false

[[bin]]
name = "sqlite3-shell"
path = "src/main.rs"

[dependencies]
sqlite3 = { path = "../../crates/sqlite3" }
